const bookInfoWidth = 300;
const bookInfoPadding = 10;
const defaultAuthorsCount = 3;
const authorInfoWidth = 120;
const authorInfoPadding = 10;
const authorsListWidth = defaultAuthorsCount * (authorInfoWidth + authorInfoPadding * 2);

export {
    bookInfoWidth,
    bookInfoPadding,
    defaultAuthorsCount,
    authorInfoWidth,
    authorInfoPadding,
    authorsListWidth
}
